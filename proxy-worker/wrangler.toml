name = "longest-night-proxy"
main = "worker.js"
compatibility_date = "2026-06-01"

# Origins allowed to call this proxy (comma-separated).
[vars]
ALLOWED_ORIGINS = "https://newdawnera.github.io"

# GEMINI_API_KEY is configured as a secret, never stored in this file.
